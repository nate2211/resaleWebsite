/**
 * ResaleMasterLab ships a fully authored CSS design system in app/globals.css.
 * Keep PostCSS enabled for Vite/Vinext, but do not run Tailwind or resolve any
 * package-style CSS imports during production builds.
 */
const config = {
  plugins: {},
};

export default config;
